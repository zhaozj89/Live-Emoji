"use strict";

let NEDITOR_MOUSE_INFO = {
	currentInput: undefined
};

let NEDITOR_SVG_CANVAS = null;

let NODE_TYPE = {
	TRIGGER : 0,
	COMPOSITE : 1,
	ACTION : 2
};

let INPUT_TYPE = {
	INPUT : 0,
	CONNECTION : 1,
	SELECTMENU : 2
};

let NEDITOR_RETURN_TYPE = {
	SUCCESS : 0,
	FAILURE : 1
};
